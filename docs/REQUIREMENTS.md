# 장바구니 BE 1단계 요구사항

## 1. 상품 기능

### 1.1 상품 목록 조회

- 사용자는 등록된 상품 목록을 조회할 수 있다.
- `GET /products` 요청으로 상품 목록을 조회한다.
- 상품 목록 조회에 성공하면 `200 OK` 상태 코드를 응답한다.
- 상품 목록은 배열 형태로 응답한다.
- 등록된 상품이 없는 경우 빈 배열을 응답한다.

### 1.2 상품 추가

- 사용자는 새로운 상품을 추가할 수 있다.
- `POST /products` 요청으로 상품을 추가한다.
- 상품 추가 시 상품명, 상품 이미지, 가격, 재고를 입력해야 한다.
- 상품 추가에 성공하면 `201 Created` 상태 코드를 응답한다.
- 생성된 상품 정보를 응답한다.

#### 상품 추가 검증

- 상품명이 누락되면 `400 Bad Request`를 응답한다.
- 상품명은 최대 100자까지 허용한다.
- 상품명이 100자를 초과하면 `400 Bad Request`를 응답한다.
- 가격이 누락되면 `400 Bad Request`를 응답한다.
- 가격은 0보다 큰 숫자여야 한다.
- 가격이 0 이하이거나 숫자가 아니면 `400 Bad Request`를 응답한다.
- 재고가 누락되면 `400 Bad Request`를 응답한다.
- 재고는 0 이상 99 이하의 정수여야 한다.
- 재고가 0 미만, 99 초과, 또는 정수가 아니면 `400 Bad Request`를 응답한다.
- 상품 이미지가 누락되면 `400 Bad Request`를 응답한다.

### 1.3 상품 삭제

- 사용자는 상품을 삭제할 수 있다.
- `DELETE /products/:productId` 요청으로 상품을 삭제한다.
- 상품 삭제에 성공하면 `200 OK` 상태 코드를 응답한다.
- 존재하지 않는 상품을 삭제하려고 하면 `404 Not Found`를 응답한다.
- 삭제하려는 상품이 장바구니에 담겨 있다면 장바구니에서도 함께 제거한다.

---

## 2. 장바구니 기능

### 2.1 장바구니 상품 목록 조회

- 사용자는 장바구니에 담긴 상품 목록을 조회할 수 있다.
- `GET /cart` 요청으로 장바구니 상품 목록을 조회한다.
- 장바구니 상품 목록 조회에 성공하면 `200 OK` 상태 코드를 응답한다.
- 장바구니 상품 목록은 배열 형태로 응답한다.
- 장바구니에 담긴 상품이 없는 경우 빈 배열을 응답한다.

### 2.2 장바구니 상품 추가

- 사용자는 상품을 장바구니에 추가할 수 있다.
- `POST /cart` 요청으로 상품을 장바구니에 추가한다.
- 장바구니에 추가할 때 `productId`와 `quantity`를 입력해야 한다.
- 장바구니 상품 추가에 성공하면 `201 Created` 상태 코드를 응답한다.
- 존재하지 않는 상품을 장바구니에 추가하려고 하면 `404 Not Found`를 응답한다.
- 이미 장바구니에 담긴 상품을 다시 추가하려고 하면 기존 장바구니 상품의 수량을 변경하지 않고 `400 Bad Request`를 응답한다.

#### 장바구니 상품 추가 검증

- `productId`가 누락되면 `400 Bad Request`를 응답한다.
- `quantity`가 누락되면 `400 Bad Request`를 응답한다.
- `quantity`는 1 이상 99 이하의 정수여야 한다.
- `quantity`가 1 미만, 99 초과, 또는 정수가 아니면 `400 Bad Request`를 응답한다.

### 2.3 장바구니 상품 변경

- 사용자는 장바구니에 담긴 상품의 수량을 변경할 수 있다.
- 사용자는 장바구니에 담긴 상품의 주문 대상 선택 여부를 변경할 수 있다.
- `PATCH /cart/:cartItemId` 요청으로 장바구니 상품 정보를 변경한다.
- 요청 body에는 변경할 `quantity` 또는 `isSelected` 중 하나 이상을 입력한다.
- 장바구니 상품 변경에 성공하면 `200 OK` 상태 코드를 응답한다.
- 변경된 장바구니 상품 정보를 응답한다.
- 존재하지 않는 장바구니 상품을 변경하려고 하면 `404 Not Found`를 응답한다.

#### 장바구니 상품 변경 검증

- `quantity`와 `isSelected`가 모두 누락되면 `400 Bad Request`를 응답한다.
- `quantity`는 1 이상 99 이하의 정수여야 한다.
- `quantity`가 1 미만, 99 초과, 또는 정수가 아니면 `400 Bad Request`를 응답한다.
- `isSelected`가 boolean 값이 아니면 `400 Bad Request`를 응답한다.

### 2.4 장바구니 상품 제거

- 사용자는 장바구니에 담긴 상품을 제거할 수 있다.
- `DELETE /cart/:cartItemId` 요청으로 장바구니 상품을 제거한다.
- 장바구니 상품 제거에 성공하면 `200 OK` 상태 코드를 응답한다.
- 삭제된 `cartItemId`를 응답한다.
- 존재하지 않는 장바구니 상품을 제거하려고 하면 `404 Not Found`를 응답한다.

### 2.5 장바구니 상품 선택

- 사용자는 장바구니 페이지에서 전체 상품을 선택하거나 선택 해제할 수 있다.
- 장바구니 페이지에 진입하면 기본적으로 전체 상품이 선택된다.
- 사용자는 장바구니 페이지에서 개별 상품을 선택하거나 선택 해제할 수 있다.
- 1개 이상의 상품이 선택된 경우 주문 확인 버튼을 활성화한다.
- 선택된 상품이 없는 경우 주문 확인 버튼을 비활성화한다.
- 사용자가 새로 고침을 해도 선택한 상품 상태를 유지한다.

### 2.6 주문 확인 페이지 이동

- 사용자는 주문 확인 버튼을 클릭하면 주문 확인 페이지로 이동할 수 있다.
- 1개 이상의 상품이 선택된 경우에만 주문 확인 페이지로 이동할 수 있다.

---

## 3. API 설계 기준

### 3.1 상품 API

| 기능           | Method | Endpoint               |
| -------------- | ------ | ---------------------- |
| 상품 목록 조회 | GET    | `/products`            |
| 상품 추가      | POST   | `/products`            |
| 상품 삭제      | DELETE | `/products/:productId` |

### 3.2 장바구니 API

| 기능                    | Method | Endpoint            |
| ----------------------- | ------ | ------------------- |
| 장바구니 상품 목록 조회 | GET    | `/cart`             |
| 장바구니 상품 추가      | POST   | `/cart`             |
| 장바구니 상품 변경      | PATCH  | `/cart/:cartItemId` |
| 장바구니 상품 제거      | DELETE | `/cart/:cartItemId` |

### 3.3 결제 금액 API

| 기능                    | Method | Endpoint       |
| ----------------------- | ------ | -------------- |
| 장바구니 결제 금액 조회 | GET    | `/cart/amount` |

### 3.4 주문 API

| 기능                | Method | Endpoint                  |
| ------------------- | ------ | ------------------------- |
| 주문 생성           | POST   | `/order`                  |
| 주문 정보 조회      | GET    | `/order/:orderId`         |
| 주문 쿠폰 목록 조회 | GET    | `/order/:orderId/coupons` |
| 주문 금액 조회      | GET    | `/order/:orderId/amount`  |
| 주문 정보 수정      | PATCH  | `/order/:orderId`         |

---

## 4. HTTP 상태 코드 사용 기준

### 4.1 성공 응답

- `200 OK`
  - 조회 요청에 성공했을 때 사용한다.
  - 기존 리소스의 일부를 수정하고, 수정된 데이터를 응답할 때 사용한다.
  - 리소스 삭제에 성공하고, 삭제된 리소스의 식별자를 응답할 때 사용한다.

- `201 Created`
  - 새로운 리소스 생성에 성공했을 때 사용한다.

### 4.2 실패 응답

- `400 Bad Request`
  - 필수 필드가 누락된 경우 사용한다.
  - 요청 값의 형식이 올바르지 않은 경우 사용한다.
  - 유효성 검증에 실패한 경우 사용한다.

- `404 Not Found`
  - 요청한 상품이 존재하지 않는 경우 사용한다.
  - 요청한 장바구니 상품이 존재하지 않는 경우 사용한다.
  - 요청한 주문이 존재하지 않는 경우 사용한다.
  - 요청한 쿠폰이 존재하지 않는 경우 사용한다.

- `500 Internal Server Error`
  - 서버 내부에서 예상하지 못한 에러가 발생한 경우 사용한다.

---

## 5. 데이터 관리 요구사항

- 데이터는 데이터베이스를 사용하지 않고 in-memory로 관리한다.
- 상품 데이터는 서버 실행 중에만 유지된다.
- 장바구니 데이터는 서버 실행 중에만 유지된다.
- 주문 데이터는 서버 실행 중에만 유지된다.
- 서버가 재시작되면 모든 데이터는 초기화된다.

---

## 6. 테스트 요구사항

### 6.1 도메인 테스트

- 상품 추가 로직을 테스트한다.
- 상품 삭제 로직을 테스트한다.
- 상품 삭제 시 장바구니에서도 함께 제거되는지 테스트한다.
- 장바구니 상품 추가 로직을 테스트한다.
- 장바구니 상품 변경 로직을 테스트한다.
- 장바구니 상품 제거 로직을 테스트한다.
- 장바구니 결제 금액 계산 로직을 테스트한다.
- 주문 생성 로직을 테스트한다.
- 주문 정보 수정 로직을 테스트한다.
- 주문 금액 조회 로직을 테스트한다.
- 주문 쿠폰 목록 조회 로직을 테스트한다.
- 상품 관련 검증 로직을 테스트한다.
- 장바구니 관련 검증 로직을 테스트한다.
- 주문 관련 검증 로직을 테스트한다.

### 6.2 API 테스트

- `GET /products` 요청을 테스트한다.
- `POST /products` 요청을 테스트한다.
- `DELETE /products/:productId` 요청을 테스트한다.
- `GET /cart` 요청을 테스트한다.
- `POST /cart` 요청을 테스트한다.
- `PATCH /cart/:cartItemId` 요청을 테스트한다.
- `DELETE /cart/:cartItemId` 요청을 테스트한다.
- `GET /cart/amount` 요청을 테스트한다.
- `POST /order` 요청을 테스트한다.
- `GET /order/:orderId` 요청을 테스트한다.
- `GET /order/:orderId/coupons` 요청을 테스트한다.
- `GET /order/:orderId/amount` 요청을 테스트한다.
- `PATCH /order/:orderId` 요청을 테스트한다.
- 정상 요청의 상태 코드와 응답 body를 테스트한다.
- 잘못된 요청의 상태 코드와 에러 응답을 테스트한다.

---

## 7. 코드 작성 요구사항

- Node.js와 Express를 사용한다.
- TypeScript로 구현한다.
- FE와 연동할 수 있도록 CORS를 설정한다.
- 변수 선언 시 `const`만 사용한다.
- 함수 또는 메서드의 들여쓰기 depth는 1단계까지만 허용한다.
- 함수의 매개변수는 2개 이하로 작성한다.
- 함수 길이는 10라인을 넘지 않도록 작성한다.
- `else`를 사용하지 않고 early return을 활용한다.
- 3항 연산자를 사용하지 않는다.
- 축약어를 사용하지 않는다.
- 클래스의 프로퍼티를 외부에서 직접 조작하지 않는다.
- HTTP Layer와 도메인 로직을 분리한다.
- 도메인 로직은 Express 요청, 응답 객체에 의존하지 않도록 작성한다.
- 가능한 순수 함수를 활용하고, 부수 효과를 분리한다.

---

## 8. 문서화 요구사항

- `docs/REQUIREMENTS.md`에 기능 요구사항과 검증 기준을 작성한다.
- `docs/api.md`에 API 명세를 작성한다.
- `docs/api.md`에는 endpoint, HTTP method, request body, response body, HTTP status code를 포함한다.
- `docs/api.md`에는 API 설계 결정 이유를 함께 작성한다.

---

## 9. 배포 요구사항

- Railway를 사용하여 서버를 배포한다.
- 배포된 서버에서 상품 API, 장바구니 API, 결제 금액 API, 주문 API가 정상 동작해야 한다.

---

# 장바구니 주문 기능 요구사항

## 10. 주문 확인 페이지

### 10.1 주문 상품 확인

- 사용자는 주문한 상품의 종류와 개수를 확인할 수 있다.
- 사용자는 주문한 각 상품의 이름, 이미지, 금액, 개수를 확인할 수 있다.
- 사용자가 주문 확인 버튼을 누르면 선택된 장바구니 상품의 `productId`와 `quantity`로 주문을 생성한다.
- 생성된 주문은 `PENDING` 상태를 가진다.
- 주문 정보에는 주문 상품 정보, 도서산간 지역 여부, 적용된 사용자 쿠폰 식별자 목록, 결제 금액 정보가 포함된다.

### 10.2 쿠폰 적용

- 사용자는 쿠폰 적용 버튼을 클릭하면 쿠폰 선택 모달을 볼 수 있다.

### 10.3 배송 지역 선택

- 사용자는 제주도 및 도서 산간 지역 여부를 체크할 수 있다.
- 도서 산간 지역 여부를 변경하면 주문 금액을 미리 조회할 수 있다.
- 도서 산간 지역 여부를 확정하면 주문 정보에 반영할 수 있다.

### 10.4 결제 금액 확인

- 사용자는 주문 금액을 확인할 수 있다.
- 사용자는 쿠폰 할인 금액을 확인할 수 있다.
- 사용자는 배송비를 확인할 수 있다.
- 사용자는 총 결제 금액을 확인할 수 있다.
- 주문 금액 조회 시 쿼리 파라미터가 없으면 저장된 주문의 쿠폰과 도서산간 지역 여부를 기준으로 금액을 확인한다.
- 주문 금액 조회 시 사용자 쿠폰 식별자 목록 또는 도서산간 지역 여부를 쿼리 파라미터로 전달하면 주문 상태를 변경하지 않고 임시 적용 금액을 확인한다.

### 10.5 결제 확인 페이지 이동

- 사용자는 결제하기 버튼을 클릭하면 결제 확인 페이지로 이동할 수 있다.
- 결제가 완료되면 주문한 상품을 장바구니에서 제거한다.

---

## 11. 쿠폰 선택 모달

### 11.1 사용 가능한 쿠폰 확인

- 사용자는 쿠폰 선택 모달에서 사용 가능한 쿠폰을 확인할 수 있다.
- 쿠폰 선택 모달에 진입하면 현재 주문에서 선택 가능한 쿠폰 후보와 사용 가능 여부를 확인할 수 있다.
- 사용할 수 없는 쿠폰은 비활성화 처리한다.
- 동일 상품을 2개 이하로 주문한 경우 해당 조건을 만족하지 못하는 쿠폰을 비활성화한다.
- 최소 주문 금액이 5만 원 미만인 경우 해당 조건을 만족하지 못하는 쿠폰을 비활성화한다.
- 최소 주문 금액이 10만 원 미만인 경우 해당 조건을 만족하지 못하는 쿠폰을 비활성화한다.
- 현재 시간이 오전 4시부터 오전 7시 사이가 아닌 경우 해당 조건을 만족하지 못하는 쿠폰을 비활성화한다.

### 11.2 쿠폰 선택

- 사용자는 사용할 쿠폰을 선택할 수 있다.
- 사용자는 최대 2개의 쿠폰을 선택할 수 있다.
- 비활성화된 쿠폰은 선택할 수 없다.
- 존재하지 않는 쿠폰 또는 현재 주문에 사용할 수 없는 쿠폰은 주문에 적용할 수 없다.

### 11.3 할인 금액 확인

- 사용자는 선택한 할인 쿠폰에 따른 할인 금액을 쿠폰 사용하기 버튼 텍스트로 확인할 수 있다.
- 선택한 사용자 쿠폰 식별자 목록으로 주문 금액을 미리 조회할 수 있다.

### 11.4 쿠폰 적용 및 모달 닫기

- 사용자는 쿠폰 사용하기 버튼을 클릭하면 선택한 사용자 쿠폰 식별자 목록을 주문 정보에 반영하고 쿠폰 선택 모달을 닫을 수 있다.
- 사용자는 X 버튼을 클릭하면 쿠폰 선택 모달을 닫을 수 있다.

---

## 12. 결제 확인 페이지

### 12.1 결제 결과 확인

- 사용자는 결제한 상품의 종류와 수량을 확인할 수 있다.
- 사용자는 총 결제 금액을 확인할 수 있다.

### 12.2 장바구니 페이지 이동

- 사용자는 장바구니로 돌아가기 버튼을 클릭하면 장바구니 페이지로 돌아갈 수 있다.
