# 요구사항 명세서

## 구현 순서

### 백엔드

1. StoredOrder, Coupon 타입 생성

- [x] 임시오더 타입
- [x] 쿠폰 타입

2. 레포 생성(get post patch 메서드)

- [] 임시오더 레포
- [] 쿠폰 레포
  - [] 쿠폰 4개 하드코딩(DB)

3. 계산 로직 구현

- [] 쿠폰 계산 로직(우선순위 로직)
- [] 최소주문 금액, 만료일, 미라클모닝 조건에 따른 비활성화
- [] 최종 할인율(쿠폰 할인 금액) , 배송비 계산 (제주도 및 도서산간 지역)

4. 결제 페이지

- [] 결제 버튼 클릭시 POST요청으로 결제 화면에서 알아야할 값들 넘겨주기
- [] 장바구니로 돌아가기 버튼 클릭시 오더테이블 아이디 삭제 (DELETE API 요청)

5. 실제 DB연결

- [] SupaBase로 인메모리 DB를 실제 DB로 변경

### 프론트엔드

1. 설계한 컴포넌트들 만들기
2. 대략적인 CSS 적용
3. `OrderConfirmPage`에서의 전역 상태 관리, `CouponModal`에서의 쿠폰 상태 관리
4. 라우터 설정
5. 리펙토링

- [] 비즈니스 로직 커스텀 훅으로 분리
- [] 가독성, 예측 가능성, 응집도, 결합도 기준으로 리펙토링

### 상태 설계

- 주문화면 페이지 상태
  - GET/order/:id (SSOT)
    - PATCH 배송정보
    - PATCH 쿠폰 사용

  - useQuery → "/order/:id"
  - 제주도서산간 → PATCH → GET/order/:id

- 쿠폰 모달 상태
  - GET "쿠폰예상할인금액" (SSOT)
    - 쿠폰 체크박스 토글

  - 쿠폰 사용 → PATCH로 수정 → GET/order/:id
  - 쿠폰 체크박스 토글 / 언토글 → GET "쿠폰예상할인금액" → 버튼에 반영

### 장바구니 (/cart)

- 주문확인을 누르면 임시오더가 생성된다.
  - 성공시 주문확인 페이지로 이동한다.

### 주문 확인 (/order-confirm/:id)

- 새로고침해도 주문확인 페이지에 남아있어야 한다
  - 임시오더를 발급한다.
  - 페이지에서 이탈하면 임시오더를 삭제한다.
- 제주도서산간 선택시 → 배송비가 3000원 추가된다.
  - 주문금액이 10만원 이상이면 무료배송이다.
  - 무료배송은 기본 3000원 할인, 제주도서산간지역은 6000원 할인이다. (할인액에는 포함안됨)
- 페이지 진입시 최대 혜택이 계산된 금액이 쿠폰 할인금액에 표시되어야 한다.
  - 선택된 쿠폰 중 '무료배송'이 있다면, 배송지정보에 따라 할인가격이 변동되어야 한다.

### 쿠폰모달

    - 기본적으로 최대혜택을 제공하는 쿠폰이 선택되어있어야 한다.
    - 체크여부에 따라 유동적으로 할인예상액이 변동되어야 한다. (할인 적용 버튼에 띄움)
    - 최대 2개 선택 가능하다.
    - MIRACLESALE 할인(정률)은 후순위 적용
    - 이미 무료배송조건을 만족하는 경우에는 무료배송쿠폰은 선택할 수 없다.
    - 최소주문금액을 만족하지 못하는 쿠폰은 선택할 수 없다
    - 사용가능시간 조건을 만족하지 못하는 쿠폰은 선택할 수 없다
    - BTGO 조건에 충족하는 상품이 없는 경우 BTGO 쿠폰은 선택할 수 없다.
    - '사용하기 버튼'을 누르지 않고 모달을 닫는 경우, OrderDB에 저장되어 있는 applied_coupon 값으로 되돌린다.

### 결제확인(결제성공되고 나서 보여야 하는 화면) (/order-success)

- 종류와 개수, 최종 결제금액 (쿠폰 적용 후)
- 주문 확인 페이지에서만 접근 가능해야함. → 뒤로가기하면 장바구니 페이지로 이동. 장바구니에서는 구매한 제품은 안보여야 함
