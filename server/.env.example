# Supabase 연결 정보. 실제 값은 server/.env 에 넣는다(커밋 금지).
# Supabase 프로젝트 > Settings > API 에서 확인.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=your-anon-or-service-key

# 서버 포트(선택)
PORT=3000
