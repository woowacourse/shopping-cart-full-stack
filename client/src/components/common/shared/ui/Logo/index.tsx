import styled from "@emotion/styled";

export default function Logo() {
  return <LogoTypo>SHOP</LogoTypo>;
}

const LogoTypo = styled.div`
  font-weight: 800;
  font-size: 20px;
  line-height: 16px;
`;
